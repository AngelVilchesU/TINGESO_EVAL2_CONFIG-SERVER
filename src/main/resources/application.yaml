server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/AngelVilchesU/TINGESO_EVAL2_CONFIG-DATA
          search-paths: config-data
  application:
    name: config-service